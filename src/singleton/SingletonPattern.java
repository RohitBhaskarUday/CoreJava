package singleton;

public class SingletonPattern {

    //The Objective of the Singleton pattern
    // is to create a single object.
    // for example
    // If we wanted to create a DB connection
    // we want it to be a single connection.
    // so with one connection we can.
    // we can make multiple query requests.

    // Different ways to do that is by
    /*
    1) Eager Initialization
    2) Lazy Initialization
    3) Synchronization Block
    4) Double check block (memory issue resolved through volatile keyword)
    5) Bill Pugh solution
    6) Enum Singleton
     */







}
